import React from "react";

function Albums() {
  return (
    <section style={styles.section}>
    </section>
  );
}

export default Albums;

const styles = {
  section: {
    display: "flex",
    flexDirection: "column",
    marginLeft: "35rem",
  },
  container: {
    display: "flex",
    flexDirection: "row",
  },
};
